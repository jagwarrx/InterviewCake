import unittestdef sort_scores(unsorted_scores, highest_possible_score):    # Sort the scores in O(n) time    numbers = {}        for i in range(highest_possible_score):        numbers[i] = 0        for i in unsorted_scores:        if i > highest_possible_score:            return 0        else:            numbers[i] = numbers[i] + 1                sorted_list = []        for i in range(highest_possible_score - 1, 0, -1):        if numbers[i] >= 1:            for j in range(numbers[i]):                sorted_list.append(i)                    return sorted_list
